# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
env/
.python-version

# IDE / editor
.vscode/
.idea/
*.swp
.DS_Store

# Claude Code local state
.claude/
CLAUDE.md.local

# Training artifacts
# Intermediate periodic snapshots — regeneratable from training.
checkpoints/dqn_intermediate/
checkpoints_dqn/
checkpoints_novamax*/
checkpoints_v3/
runs_v4/
# Only the deployed / canonical model weights are tracked; experiment-variant
# checkpoints are regenerable from training and stay local.
checkpoints/*.pt
!checkpoints/dqn3d_stack_stageA_best.pt
!checkpoints/ppo_robust_best.pt
!checkpoints/dqn_3d_bc_actor_best.pt
!checkpoints/dqn_3d_bc_actor_final.pt
!checkpoints/dqn_3d_bc_oldphys_best.pt
# BC datasets are large + regenerable in Phase 0a (~5 min each).
data/*.npz
# LITE backend local caches/artifacts (regenerable; never committed).
data/registry/
data/jobs/
# Training logs.
logs/
*.log
# Old stable-baselines3 zips.
*.zip

# Misc
*.tmp
timings_*.json
